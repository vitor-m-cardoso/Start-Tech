// function 'normal'.
function soma(x, y) {
  return x + y;
}

// arrow function ES6.
const dividir = (x, y) => x / y;

// arrow function que retorna um objeto.
const obj = () => ({ nome: 'Vitor', sobrenome: 'Cardoso' });